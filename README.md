# ToDoList
A modern Task Management system built with a .NET Minimal API backend and a React frontend. The project implements a clean Microservices-ready architecture, utilizing Entity Framework Core with a DB-First approach to interface with a MySQL database.
